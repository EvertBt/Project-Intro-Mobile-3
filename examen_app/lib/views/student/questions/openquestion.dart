import 'package:flutter/cupertino.dart';

Widget openQuestion(BuildContext context) {
  return Text("Open ended question!");
}

Widget openQuestionAnswer(BuildContext context) {
  return Text("Open ended answer!");
}
